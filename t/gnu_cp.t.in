#!/usr/bin/perl

use strict;
use Test::More tests => 1;
use SysWrap;

# check for GNU cp
my @lines = `@CP@ --help | grep -i 'gnu'`;

SKIP: {
	skip('GNU cp not found', 1) if (@lines == 0);

	ok(0 == rsnapshot("-c t/support/etc/gnu_cp.conf hourly"));
}
